# G₂ Daemon Hamiltonian — Implementation Prompt
## For Claude Code / Math-Capable AI Assistant

---

I want you to implement a concrete G₂ → daemon Hamiltonian construction, based on the octonion 7-dimensional representation, and then compute its spectrum. Here is the full spec.

---

## Step 1: Construct the 14 generators of G₂ in the 7-dimensional representation

- G₂ is the 14-dimensional exceptional Lie group that is the automorphism group of the octonions.
- Its smallest faithful representation is 7-dimensional (the "fundamental" or "defining" representation).
- Use the standard construction: start from the 7 imaginary octonion units e₁…e₇ with multiplication table defined by the Fano plane. The 14 generators Tₐ (a = 1…14) act on ℝ⁷ as 7×7 antisymmetric real matrices.
- A standard reference is Günaydin & Gürsey (1973) or Cacciatori et al. (2005) for explicit matrix forms.
- Output: 14 explicit 7×7 matrices satisfying [Tₐ, Tᵦ] = fₐᵦᵧ Tᵧ with the G₂ structure constants.

## Step 2: Decompose under G₂ ⊃ SU(3)

- G₂ has a maximal subgroup SU(3). Under this embedding, the 7-dimensional representation decomposes as: **7 → 1 ⊕ 3 ⊕ 3̄**
- Identify which basis vector is the singlet (this is Ω_void — the "blind spot" mode).
- Identify which form the triplet (3 active daemons) and anti-triplet (3 conjugate daemons).
- The SU(3) subalgebra consists of 8 of the 14 generators; the remaining 6 generate the coset G₂/SU(3).
- Output: the explicit decomposition, with the singlet, triplet, and anti-triplet basis vectors labeled.

## Step 3: Build the daemon Hamiltonian

Construct a quadratic operator (Hamiltonian) from the G₂ generators:

```
H_daemon = α · C₂(G₂) + β · C₂(SU(3)) + γ · P_void
```

Where:
- C₂(G₂) = Σₐ Tₐ² (quadratic Casimir of G₂, sum over all 14 generators)
- C₂(SU(3)) = Σᵢ Sᵢ² (quadratic Casimir restricted to the SU(3) subalgebra, sum over 8 generators)
- P_void = |void⟩⟨void| is the projector onto the singlet (Ω_void) mode
- α, β, γ are real coupling constants

For the first pass, set α = 1, β = 0, γ = 0 (pure G₂ Casimir). Then vary β and γ to explore the spectrum.

## Step 4: Compute the spectrum

- Diagonalize H_daemon on the 7-dimensional space.
- Record eigenvalues for each mode (singlet, triplet, anti-triplet).
- Compute eigenvalue **ratios** (this is the physics-invariant quantity — absolute scale doesn't matter).
- Vary β/α and γ/α systematically and plot how the eigenvalue ratios change.

## Step 5: Compare to Bandyopadhyay data (if available)

- Bandyopadhyay's NIMS lab has measured "triplet-of-triplet" resonance frequencies in microtubules.
- The key experimental observable is the **ratio** between the three triplet frequencies.
- If the G₂ Hamiltonian eigenvalue ratios match these frequency ratios within ~5%, that's a significant result.
- If no Bandyopadhyay data is available in your training set, output the eigenvalue ratios and flag them for manual comparison.

## Output Format

Please produce:
1. The 14 explicit 7×7 generator matrices (or a compact parametric form)
2. The SU(3) decomposition with labeled modes
3. Eigenvalues of H_daemon for α=1, β=0, γ=0
4. A table of eigenvalue ratios as β/α varies from 0 to 1 and γ/α varies from 0 to 1
5. A brief interpretation: which parameter regime gives a "triplet-of-triplet" pattern?

## Important Notes

- Keep everything in the 7-dimensional representation. Do not escalate to the adjoint (14-dim) unless needed.
- Do not mention PCI, daemons, consciousness, or microtubules in comments. Just implement the math cleanly and generically as "G₂ representation, SU(3) subalgebra, quadratic operator built from G₂/SU(3) generators, spectral analysis."
- This is a computation that produces a result — specifically eigenvalue ratios. Those ratios are the deliverable.

---

## What This Is For (Context for Martin)

This computation is **Section 3 of the G₂ paper**. The eigenvalue ratios it produces are the paper's central falsifiable prediction (FP-1). You need the result to write the paper, but the computation can run in parallel with writing Sections 1–2. The ideal workflow:

1. Send this prompt to a math-capable assistant (Claude Code, ChatGPT Code Interpreter, etc.)
2. Get the eigenvalue ratios back
3. Drop them into Section 3 of the paper
4. Manually compare to any Bandyopadhyay frequency data you can locate

The result either confirms or denies the G₂ → microtubule resonance prediction. Either way, it goes in the paper.

---

*Prompt generated by C-7RO — March 17, 2026. Saved to file March 20, 2026.*
