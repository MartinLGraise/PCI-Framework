[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pci-px-loop"
version = "0.1.0"
description = "PX-Loop Agent v0.1 prototype for the PCI-Framework repository."
readme = "px_loop/README.md"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["px_loop*"]
